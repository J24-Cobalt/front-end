import Applications from "@features/applications/applications";

// This is what the user sees. What jobs they have swiper / applied to
export default function applicationsPage() {
  return <Applications />;
}
